extends CanvasLayer


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#var dt = StoryManager.get_date()
	#print(StoryManager.display_date(dt))
	
	#var dt = Time.get_unix_time_from_system()
	#print(dt)
	#var dtt = Time.get_datetime_dict_from_unix_time(dt)
	#print(dtt)
	#var dttt = StoryManager.get_date(dt)
	#print(dttt)
	#var test: Stories = Stories.new()
	#print(test.pk_id)
	#test.pk_id = 12
	#print(test.pk_id)
	
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
